CREATE TABLE users (
                       user_id VARCHAR(255),
                       login VARCHAR(255),
                       first_name VARCHAR(255),
                       last_name VARCHAR(255)
);